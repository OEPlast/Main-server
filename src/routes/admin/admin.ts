import express from 'express';
const router = express.Router();

router.get('/admin', () => {});
router.post('/admin', () => {});
router.put('/admin/:id', () => {});
router.delete('/admin/:id', () => {});

export default router;
