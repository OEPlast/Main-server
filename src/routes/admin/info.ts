import express from 'express';
const router = express.Router();

router.get('/info', () => {});
router.post('/info', () => {});
router.put('/info/:id', () => {});
router.delete('/info/:id', () => {});

export default router;
