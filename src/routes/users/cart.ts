import express from 'express';
const router = express.Router();

router.get('/cart', () => {});
router.post('/cart', () => {});
router.put('/cart/:id', () => {});
router.delete('/cart/:id', () => {});

export default router;
